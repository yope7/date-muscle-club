# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"
      - name: Install Dependencies
        run: npm ci --legacy-peer-deps
      - name: Create .env file
        run: |
          echo "NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSyB3VrSH5rtQqm2C0vPVAubhZxXGZgwfLbg" >> .env
          echo "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=date-muscle-club.firebaseapp.com" >> .env
          echo "NEXT_PUBLIC_FIREBASE_PROJECT_ID=date-muscle-club" >> .env
          echo "NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=date-muscle-club.firebasestorage.app" >> .env
          echo "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=148397492251" >> .env
          echo "NEXT_PUBLIC_FIREBASE_APP_ID=1:148397492251:web:49ed3676e84a36387bd0be" >> .env
          echo "NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-5ETKECCJ4Q" >> .env
      - name: Build
        run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DATE_MUSCLE_CLUB }}
          channelId: live
          projectId: date-muscle-club
